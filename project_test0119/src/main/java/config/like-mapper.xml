<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="review_like">

	<select id="like_counts" parameterType="Int" resultType="Int">
		select
		count(*) from review_like where review_like_review_seq =
		#{review_like_review_seq}
	</select>

	<insert id="like_insert" parameterType="Review_likeDTO">
		INSERT INTO
		review_like(review_like_seq, review_like_user_id,
		review_like_review_seq)
		VALUES(review_like_sequence.nextval, #{review_like_user_id}, #{review_like_review_seq})
	</insert>

	<delete id="like_delete" parameterType="Int">
		DELETE FROM review_like
		WHERE review_like_review_seq = #{review_like_review_seq}
	</delete>
</mapper>